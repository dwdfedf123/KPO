# 🧪 Юнит‑тесты для проекта «Трекер Учёбы»

В этом каталоге находятся юнит‑тесты, проверяющие корректность работы ключевых модулей приложения: аутентификация, база данных, а также клиентские обёртки для внешних API (мотивация, время, расписание). [file:115]

## 📁 Содержание

- **`test_auth.py`** — тесты регистрации и входа пользователя, обработка типовых ошибок (неверный пароль, дублирование имени). 
- **`test_database.py`** — проверка операций с SQLite: пользователи, учебные сессии, достижения. 
- **`test_api_motivation.py`** — тесты получения мотивационной цитаты (успех/ошибка) с подменой ответа внешнего сервиса. 
- **`test_api_time.py`** — тесты получения текущего времени из API и корректной обработки ответа. 
- **`test_api_schedule.py`** — тесты получения и обработки расписания группы через API (включая сценарии с ошибкой и fallback‑данными). 

## ▶️ Запуск тестов

Запускать из корня проекта (там, где лежат папки `program_files/` и `tests/`): 

```bash
python -m unittest discover -s tests
```
Для более подробного вывода: 
```
bash
python -m unittest discover -s tests -v
```
